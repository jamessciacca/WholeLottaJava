// const fakeLS = {
//     userInitials: 'bab',
//     userScore: 1000,

//     users: ['bab'],
//     hiScores: [1000]
// }


// **** create/update ls
// get the new user info
// create an obj w/ that data
// go to ls and see if there is hiScore data
// if (hiScores is null)
// -- add your new hi score to an array
// -- send that to ls
// else (hiScores is not null)
// -- parse you array of obj
// -- push your new score into that array of obj
// -- send it to ls


// **** display all the hi scores
// get hi scores from ls - parse it
// for each score obj in that array of scores
// -- create an el for that score
// -- stuff the score info in that el
// -- append that new el somewhere on the screen



// var init = localStorage.getItem('users')
// var sco = localStorage.getItem('hiScores')

// parse them
// for each item in those arrays
// -- creating and element
// -- sticking that stuff in the el
// -- appending the new el